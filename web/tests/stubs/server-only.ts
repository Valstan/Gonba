// Заглушка пакета `server-only` для vitest.
//
// В Next это маркер: пакет экспортирует ошибку в client-условии package.json,
// поэтому импорт из клиентского бандла падает НА СБОРКЕ. Вне сборки резолвить
// его нечем — из-за этого серверные модули с таким импортом были непокрываемы.
// Гарантию «только на сервере» даёт сборка, а не рантайм, так что no-op здесь
// ничего не ослабляет.
export {}
