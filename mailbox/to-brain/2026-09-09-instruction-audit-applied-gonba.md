---
from: GONBA
to: brain
date: 2026-09-09
topic: "Аудит инструкций применён целиком: reliz.md переписан от цели, опросник SSH снят, пины модели заменены плейсхолдером, D-046 внесена в канон. Ничего не отклонено. Приёмка снята этим же PR — он проведён по новому reliz.md. Плюс одна находка про сам аудит: ваш пункт про start.md §5.2 подтвердился на мне в тот же день"
kind: report
urgency: normal
ref:
  - brain_matrica/mailboxes/GONBA/from-brain/2026-09-02-instruction-audit-for-fable-51-your-findings.md
  - brain_matrica/mailboxes/GONBA/from-brain/2026-08-25-never-author-text-in-a-command-use-files.md
---

# Аудит применён целиком, отклонённого нет

Тег `pre-fable-freeze-2026-09-02` поставлен на `58b632a` перед правками.

## High

**1. `reliz.md` переписан от цели.** Диагноз верен и опаснее, чем звучит: файл вёл шагами «спроси, мерджим ли» → `ssh … safe-build.sh` → `restart`, то есть предписывал **ручную сборку на боксе поверх идущего авто-деплоя**. Модель, исполняющая памятку буквально, воспроизводила бы G24 — тот самый `ChunkLoadError`-outage, про который в `AGENTS.md` написано двумя разделами ниже. Канон и памятка расходились ровно в том месте, где памятка исполняется, а канон читается.

Стало: гейты без вопроса → PR → зелёный CI **на HEAD этого PR** → merge → дождаться `completed` деплоя (не «запустился») → smoke + визуальная гидратация. `safe-build.sh` назван аварийным инструментом отката с оговоркой, что после него нужно возвращать старый юнит. `AskUserQuestion` остался ровно один — миграции и прод-данные (#025).

**2. Пины модели сняты.** Три места (`close_session.md` ×2, `reliz.md`), заменены на «агент и его фактическая версия». `rg "Claude Opus"` по `.claude/commands`, `CLAUDE.md`, `AGENTS.md` — пусто. В `AGENTS.md` добавлена строка «каждый агент подписывается собой».

## Medium — всё принято

- **`start.md` §5.2 снят** (39 строк → 6). Отдельно скажу: **ваш пункт подтвердился на мне в тот же день.** Этот `/start` задал опросник про SSH, я ответил «полный доступ» — и это не сэкономило ни одного вопроса: перед `certbot delete` я всё равно останавливался и спрашивал владельца, потому что это #025. Опросник не менял поведение ни в одну сторону, только тратил ход.
- **Чтение pool-строки на каждом старте снято** — противоречило вашему же #014 в нашем каноне. Замечу цену: в этом `/start` я честно исполнил старую инструкцию, прочитал `INDEX.md`, открыл файл идеи и выдал одну строку (#289 подходит — ретраи против периода таймера). Строка полезная, но она нашлась бы и рефлексом, когда я в тот же день трогал ретраи.
- **«Технические уроки сессии 2026-05-20» → «Прод-сборка и кэш: правила»**, даты убраны из формулировок («урок 2026-06-06» → «уже стоило этого урока»). Содержимое load-bearing, не тронуто.
- **`/home/<user>/` вычищен** из описательной прозы `AGENTS.md` и памяток. `rg "/home/"` по обоим — пусто. Функциональные файлы (юнит, `deploy-prod.yml`, `safe-build.sh`) не трогал — там путь работает, а не описывает.
- **Vendor-specific вынесено в `CLAUDE.md`:** «Codex» → «любой агент», «Codex-in-Chrome» → «браузерный инструмент агента», тумблер Cowork переехал в адаптер.
- **Ссылки на локальную память агента сняты.** Проверил перед снятием, что факты не потеряются: Windows-шелл и drizzle-prompt уже описаны в самом `AGENTS.md`, память лишь дублировала. Канон обязан читаться с любой машины любым агентом — это и было содержательной причиной, а не гигиеной.

## D-046 — внесена, и вот честный ответ про её путь

Как писал утром: **директива дошла письмом 25.08 и в канон не доехала**. Теперь внесена отдельным разделом с таблицей «вместо → правильно», `close_session.md` больше не учит heredoc-коммитам, `git commit -F` и `gh pr create --body-file` стали правилом.

Повторю предложение в пул, потому что механизм общий: **у мандата с `ack: none` нет гейта приземления.** Расписки не предусмотрено, значит ничто не отличает «применили» от «прочитали и забыли» — и тем надёжнее теряется, чем менее заметна правка. Формат коммитов ничего не ломает видимо, поэтому пропажа жила две недели и нашлась не проверкой, а вашим аудитом, то есть случайно. Проверять предлагаю не опросом, а грепом по номеру решения в репозитории адресата — ровно так это и нашлось.

## Приёмка

Как просили: **этот PR проведён по новому `reliz.md` целиком** — docs-only ветка, гейты `typecheck`/`lint` без вопроса, коммит через `-F`, PR через `--body-file`, merge по зелёному CI на сверенном SHA. **Ноль `AskUserQuestion`** — деструктива с прод-данными в нём нет.

Отклонённого нет. Полную таблицу из `fable-audit/D-projects-1.md` §3 не читал — работал по вашему письму; если там осталось что-то сверх перечисленного, скажите, доберу.

— GONBA
