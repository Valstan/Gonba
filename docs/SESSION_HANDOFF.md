# Session Handoff

**Status:** ACTIVE
**Updated:** 2026-07-10
**Branch:** main
**Last released version:** PR #161 (`8b1261e`, «Народная лента» UGC — этап 1). Прод здоров, `/lenta` live (health 200, чанки 200, degraded S3).

---

## Текущая нитка

**Заимствования из Sabantuy → Гоньба** (оживление сайта, план [`docs/plans/sabantuy-borrowings.md`](plans/sabantuy-borrowings.md)). Владелец одобрил 4 пакета; идём по этапам.

- **Этап 1 — Народная лента (UGC)** ✅ **на проде 2026-07-10** (PR #161). Посетители выкладывают фото/видео прямо на сайте (`/lenta`): presigned-PUT напрямую в Yandex Object Storage мимо бокса, постмодерация (стоп-фильтр мата + жалобы→авто-скрытие + rate-limit), аноним-владение по браузерному токену, лайки/комменты/просмотры, лайтбокс/камера/«Смотреть подряд». Миграция `20260710_120000` применена (batch 11; FK дочерних CASCADE — анти-G135). e2e зелёный.
  - **⚠️ Degraded до S3-кредов:** загрузка отдаёт 503, пока владелец не заведёт бакет + `S3_BUCKET`/`S3_ACCESS_KEY_ID`/`S3_SECRET_ACCESS_KEY` в `/etc/gonba/gonba.env` (+ `restart gonba`). Всё остальное (страница, лента, модерация) работает.

## Следующий шаг

Продолжить план: **Этап 2 — Фотостена + фотобатл** (masonry всех одобренных кадров, пары-голосование, лидерборд месяца). Затем этапы 3 (афиша + «Моя программа»), 4 (викторина-краеведение), 5 (VK-вход + web-push + PWA). Спросить владельца, продолжать ли сразу или ждать S3-креды для этапа 1.

## Контекст

- **VK-синк на проде:** все 4 источника в ошибке — **токены `VK_TOKEN_VALSTAN`/`VK_TOKEN_VITA` протухли** (VALSTAN «user is blocked», VITA «invalid access_token»). Механизм цел, `/api/health` показывает `vkSync.healthy:false`. Нужны свежие VK-токены от владельца (это отдельная задача, не блокер ленты).
- **Связанные коммиты сессии:**
  - `c559952` (#160) — разбор почты brain: ADR-0007 в CLAUDE.md, G135-скан (6 FK-мин в PENDING), re-триаж #033.
  - `8b1261e` (#161) — Народная лента UGC (этап 1). ~6.2k строк: 6 коллекций + server/ugc + компоненты Lenta + миграция.
- **Прод:** ✅ `8b1261e`, health 200. `/lenta` 200, чанки 200 (нет ChunkLoadError), sign-upload 503 (degraded, ожидаемо).
- **Деплой этой фичи:** авто-деплой упал на safety-net (новая миграция — by design); миграция применена вручную; деплой доведён `workflow_dispatch` (deny снят на сессию с разрешения владельца, тут же возвращён).
- **Открытые вопросы для пользователя:** (1) S3-креды для включения загрузки; (2) продолжать этап 2 сейчас или ждать; (3) свежие VK-токены (синк стоит).

## Не забыть (low-priority)

- 🟢 **S3-креды** для `/lenta` (см. выше) — до них загрузка degraded.
- 🟢 **VK-токены протухли** — синк постов стоит с ~10 июля.
- 🟡 **G135** — 6 FK-мин в прод-схеме (PENDING); UGC-таблицы этой фичи уже сделаны с CASCADE (мины там нет).
- ⏰ #036 Триггер-2 (квартальный самоосмотр) — `due 2026-08-03` (в PENDING).

---

> Sticky note — что было следующим шагом. Перезаписывается через `/close_session`.
