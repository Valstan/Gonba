# Session Handoff

**Status:** IDLE
**Updated:** 2026-08-10
**Branch:** main
**Last released version:** PR #199 (`39e4099`); CI, deploy и прод-проверки зелёные.

---

## Текущая нитка

Нитки нет — всё, что было взято в работу, доведено до конца и проверено на проде. Закрыта срочная очередь brain от 09–10.08: заведён счётчик Яндекс.Метрики `111457955` с приёмкой по живым визитам в кабинете, LiveInternet вычеркнут из кода (D-025), видимый информер посещаемости встал в подвал всех страниц через собственный прокси, закрыт G234 (keep-alive + graceful drain на SIGTERM) и досрочно проведена ротация SSH deploy-key.

## Следующий шаг

Свободное состояние, ждём задачу. Кандидаты в порядке, который назвал владелец:

1. **Диагностика VK auto-sync в админке** — brain поставил её первой из функциональных (риск неверных привязок нашёл сам квартальный аудит, источников теперь шесть).
2. **Свести доки pnpm→npm** — `AGENTS.md` (в т.ч. формулировка гейта автономии, ~180/187/220), `docs/PROJECT.md` (~188–306), `docs/PROJECT_STATE.md` (~130) и слэш-команды `/check`, `/reliz`, `/obriv`; решить судьбу `corepack pnpm run *:raw` внутри watchdog-обёрток в `web/package.json`, удалить протухший `web/pnpm-lock.yaml`, обновить memory `windows_pnpm_setup`.
3. **Разобраться с `SECRETS_TOKEN`** — подтвердить у владельца происхождение значения, активировать vault-контур на боксе и разблокировать перевод VK-классификатора на DeepSeek (D-024).
4. **Контентная чистка VK-постов** — только через Payload Local API и с отдельным подтверждением (#025).

## Контекст

- **План:** —
- **Связанные коммиты сессии:**
  - `3008e7b` (#197) — счётчик Метрики, информер через свой origin, удаление LiveInternet, G234 (таймауты + SIGTERM-drain).
  - `612684d` (#198) — ротация SSH deploy-key, сверка `authorized_keys` прода, правка протухшей строки в таблице dev-машин.
  - `39e4099` (#199) — закрытие ротации: старый ключ снят после зелёного деплоя.
- **Прод:** релиз `39e4099` активен; health 200, `vkSync.healthy=true` (6 источников, 0 ошибок, 0 просрочек). `/api/analytics-config` отдаёт `{"ymCounterId":111457955}`, `/api/analytics-informer` — 200 `image/png` с живой цифрой, `/api/analytics-stats` — 307 на публичную статистику счётчика. В логе сервиса `[http-lifecycle] keepAliveTimeout=65000мс headersTimeout=66000мс drain=15000мс`. В `authorized_keys` остались `gonba-deploy@PC40-20260810` (основной, GH secret) и `gonba-deploy@HOME-PC-20260529` (машина B).
- **Открытые вопросы для пользователя:** происхождение значения GH secret `SECRETS_TOKEN` (появился 2026-08-10 10:29Z, файла `/etc/gonba/secrets-token.env` на боксе нет); подтверждение на контентную чистку постов/медиа.

## Не забыть (low-priority)

- Информер Метрики обновляется на стороне Яндекса с задержкой — наш прокси отдаёт бейдж байт-в-байт, расхождение цифры с кабинетом в первые минуты не баг.
- Внешний alert по повторному all-fail SARAFAN остаётся в `docs/PENDING_FOLLOWUPS.md`.
- Остаток #036: 55 over-export, 20 exported types, 2 duplicate exports — brain разрешил чистить партиями.
- G135: 6 FK-мин «NOT NULL + ON DELETE SET NULL» ждут per-FK design-решения (пункт за порогом старения, предложен re-триаж).

---

> Sticky note — свободное состояние. Ротация SSH-ключа выполнена, следующая до **2026-11-08**. Любые живые Payload-данные менять только через Local API и с отдельным подтверждением.
