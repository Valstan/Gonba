# Session Handoff

**Status:** ACTIVE
**Updated:** 2026-09-09
**Branch:** main
**Last released version:** `58b632a` (#221); деплой зелёный, прод на нём, health 200.

---

## Текущая нитка

**Разбор очереди brain 01–06.09 (была просрочена).** Сессию не открывали восемь дней, накопилось восемь непрочитанных писем, среди них три MUST и мандат владельца с наступившим сроком. Очередь закрыта целиком; попутно чек-лист brain нашёл **две настоящие мины в нашем деплое** (обе починены), а аудит инструкций под Fable 5.1 применён целиком.

Продуктовая нитка «правила редакции для VK-классификатора» **никуда не делась и ждёт** — она и есть следующий шаг.

## Следующий шаг

**Приёмка первого живого прогона VK-классификатора с правилами** — не начата, перенесена из прошлого handoff'а. Глобал `vkEditorialRules` наполнен на проде с 01.09, таймер бил каждые 3 ч восемь суток, так что данных много. Смотреть **не «прогон зелёный», а вердикты**:

```bash
ssh GONBA 'set -a; . /etc/gonba/gonba.env; set +a; psql "$DATABASE_URL" -c "
  SELECT id, left(title,50) AS title,
         vk_classification->>'"'"'provider'"'"' AS provider,
         vk_classification->>'"'"'projectSlugs'"'"' AS projects,
         left(vk_classification->>'"'"'rationale'"'"', 90) AS rationale
  FROM posts WHERE vk_classification IS NOT NULL
  ORDER BY id DESC LIMIT 5;"'
```

Гипотеза одна: **записи из общей группы села должны перестать оседать на `gonba`** (по разметке владельца туда идёт примерно одна из девятнадцати). Дальше по порядку, не перепрыгивать: приёмка → расхождения править **текстом правил в админке**, не кодом → и только потом запрет, с одного источника и первым шагом без запрета.

## Контекст

- **Сделано этой сессией** (два PR): осиротевшие линии certbot удалены (мандат владельца, совпадение имён проверено до удаления, приёмка тремя частями); apt/tzdata на боксе чисты, PG на `Etc/UTC`, а не на хостерском `Host`; G231 — 24 = 24, мины нет; `/obriv` снят и добавлен `SessionStart`-хук (D-066); **две правки деплоя** — см. ниже; письмо brain по всем восьми пунктам. Вторым PR — аудит инструкций: `reliz.md` переписан от цели (предписывал ручной build поверх авто-деплоя), опросник SSH снят, пины модели заменены плейсхолдером, D-046 внесена в канон. Точка отката — тег `pre-fable-freeze-2026-09-02`.
- **Две мины в `deploy-prod.yml`, найденные чек-листом brain и починенные:**
  - **G322:** `ssh … "npm install … | tail -5"` — конвейер на удалённой стороне, код выхода отдаёт `tail`, упавший install уезжал зелёным. Ломались бы таймеры `vk-sync`/`media-cache`, то есть не сайт → нашлось бы поздно.
  - **G321:** `ssh 'bash -s' "$SHA"` при пустом аргументе давал `REL="$BASE/releases/"`, и следующая строка `rm -rf` снесла бы **весь каталог релизов вместе с откатом**. Добавлен гвард.
  - Обе проверены **контроль-негативом**, не глазами: без `pipefail` — код 0 на упавшей команде, с ним — 7; гвард на пустом SHA — выход 1 до деструктивной строки.
- **Прод:** релиз `58b632a`, health 200, все семь имён бокса отдают свой контент. `systemctl --failed` содержит `fwupd`/`fwupd-refresh` (владелец решил не маскировать) и `kalinino-vk-sync` — **юнит соседа**, не наш.
- **Разметка владельца** для правил редакции — на странице-артефакте, читается обратно через Artifact `read`: <https://claude.ai/code/artifact/635d7385-0ee2-4bce-9e03-876c46b05e29>. Единственный экземпляр 30 размеченных вердиктов.
- **Открытые вопросы для владельца:**
  - Запрет включать после приёмки прогона — согласовать, **с какого источника** начинать.
  - Артефакт (б) «правила для агента» (`AGENTS.md` + `web/.cursor/rules/`) — просил 26.08, не начат.
  - `fwupd` маскировать или оставить — brain рекомендует замаскировать, решение отложено.

## Failed approaches (этой нитки)

- **Закрыть проверку vhost'ов фразой «Ubuntu грузит только `sites-enabled`»** — соглашение вместо свойства. `grep` дал совпадение в файле без симлинка, и соблазн был списать это на конвенцию. Прочитаны фактические `include` в `nginx.conf` и `ssl_certificate` каждого включённого vhost'а. **Не повторять:** перед удалением чужого TLS-артефакта проверяется конфиг, а не дистрибутивная привычка.
- **Снять `certbot.service` из `--failed` через `reset-failed`** — быстро и выглядит как приёмка. Отклонено: это прячет отказ, а не чинит его. Юнит запущен по-настоящему и дал `Result=success`.
- **Считать `apt list --upgradable` мерой того, что apt доработал** — 8 пакетов выглядят как штатное накопление и как хвост оборванного прогона **одинаково**. Различает только `dpkg --audit`.
- **Брать список коллекций грепом по `web/src/collections/`** для G231 — дало бы 19 против 24 и **ложную тревогу** с починкой здоровой схемы. Пять коллекций приходят от плагинов и из встроенного функционала, а не из каталога. Список — только из `payload.config.ts` + плагинов.

## Не забыть (low-priority)

- **`ack: none` у мандата = нет гейта приземления.** D-046 дошла письмом 25.08 и в канон не доехала; нашлось случайно, через чужой аудит две недели спустя. Предложено brain как класс.
- **`shell: bash` не объявлен ни в одном шаге `.github/workflows`** — отсюда и отсутствие `pipefail`. Правка сделана точечно в двух местах; сплошного прохода по остальным шагам **не было**.
- **npm на Windows берёт cmd.exe** — `npm run typecheck` падает на `NODE_OPTIONS`. Звать с `--script-shell="C:\Program Files\Git\bin\bash.exe"`.
- **Гард интервала съедает каждый второй прогон VK-синхронизации** — требует выбора из трёх лечений. Плюс pool #289: ретраи `VK_MAX_IMPORT_ATTEMPTS = 3` добавлены 01.09 и **не пересчитаны против трёхчасового периода таймера**.
- **Доля пустых ответов DeepSeek не измерена** — мерить до починки.
- **Сетевой путь до бокса отсюда деградирован** — внешние замеры задержки недостоверны, контент приходит верный.

---

> Sticky note — очередь brain закрыта, деплой стал на две мины безопаснее, слой инструкций больше не ведёт в outage. Дальше — приёмка вердиктов классификатора: она ждёт с 01.09 и данных за восемь суток накопилось много.
